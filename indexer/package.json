{
  "name": "propvera-subgraph",
  "version": "1.0.0",
  "description": "Subgraph for indexing PropVera contract events on Cronos Testnet",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.goldsky.com/api/public/project_cl6yxxxxxx/subgraphs/name/version/0.0.1",
    "create-local": "graph create --node http://localhost:8020/ propvera-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ propvera-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 propvera-subgraph"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.68.0",
    "@graphprotocol/graph-ts": "^0.32.0"
  }
}